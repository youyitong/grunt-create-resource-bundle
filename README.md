grunt-create-resource-bundle
============================

Iterate over all subfolders under source_dir, concatenate all the javascript files under each subfolder and put it under dist_dir with prefix
